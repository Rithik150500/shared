"""Operator-facing helper scripts for whatsapp_delivery.

Scripts in this package are one-shot tools the human operator runs
manually at launch / approval time -- they are NOT part of the
production request path. See `submit_templates_to_meta.py` for the
Meta template submission workflow.
"""
